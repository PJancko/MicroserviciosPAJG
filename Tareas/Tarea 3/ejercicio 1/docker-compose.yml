version: "3.8"

services:
  app:
    build: .
    container_name: mi_app_node
    ports:
      - "3000:3000"
    environment:
      - MONGO_URI=mongodb://mongo:27017/mi_base
    depends_on:
      - mongo
    volumes:
      - .:/app
      - /app/node_modules

  mongo:
    image: mongo:7
    container_name: mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
